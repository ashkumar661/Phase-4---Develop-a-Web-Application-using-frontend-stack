# AngularQuizApp

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 14.1.0.

This is a simple quiz project. It accepts questions from json file and renders it. There are 10 questions with negative scoring along with the timmer. At the end it displays score card.